{% render 'partials/headsprint.liquid'%}
{% render 'partials/header.liquid', title: "sprint 9" %}

<body>
     

    <main>
        <h2>Sprint 11: Pleasurable UI</h2>

        <section>
          {% for file in files %}
            <article>
                    <a href="/sprint11/{{ file | slice:0, -3}}">{{ file | slice:0, -3}}</a>
            </article>
          {% endfor %}
        </section>
    </main>