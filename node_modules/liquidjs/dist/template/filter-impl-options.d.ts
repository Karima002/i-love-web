import type { Context } from 'liquidjs/dist/context';
import type { Liquid } from 'liquidjs/dist/liquid';
import type { FilterToken } from 'liquidjs/dist/tokens';
export interface FilterImpl {
    context: Context;
    token: FilterToken;
    liquid: Liquid;
}
export type FilterHandler = (this: FilterImpl, value: any, ...args: any[]) => any;
export interface FilterOptions {
    handler: FilterHandler;
    raw: boolean;
}
export type FilterImplOptions = FilterHandler | FilterOptions;
