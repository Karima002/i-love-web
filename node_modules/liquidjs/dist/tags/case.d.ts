import { ValueToken, Liquid, Value, Emitter, TagToken, TopLevelToken, Context, Template, Tag } from 'liquidjs';
import { Parser } from 'liquidjs/dist/parser';
import { Arguments } from 'liquidjs/dist/template';
export default class extends Tag {
    value: Value;
    branches: {
        values: ValueToken[];
        templates: Template[];
    }[];
    elseTemplates: Template[];
    constructor(tagToken: TagToken, remainTokens: TopLevelToken[], liquid: Liquid, parser: Parser);
    render(ctx: Context, emitter: Emitter): Generator<unknown, void, unknown>;
    arguments(): Arguments;
    children(): Generator<unknown, Template[]>;
}
