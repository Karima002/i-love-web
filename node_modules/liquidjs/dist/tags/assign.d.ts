import { Liquid, TopLevelToken, TagToken, Context, Tag } from 'liquidjs';
import { Arguments } from 'liquidjs/dist/template';
import { IdentifierToken } from 'liquidjs/dist/tokens';
export default class extends Tag {
    private key;
    private value;
    private identifier;
    constructor(token: TagToken, remainTokens: TopLevelToken[], liquid: Liquid);
    render(ctx: Context): Generator<unknown, void, unknown>;
    arguments(): Arguments;
    localScope(): Iterable<IdentifierToken>;
}
